name: sk_works
description: SKO company and work management app
publish_to: 'none'
version: 0.2.0+2

environment:
  sdk: '>=3.9.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  file_picker: ^8.3.7
  geolocator: ^14.0.3
  image_picker: ^1.1.2
  local_auth: ^2.3.0
  shared_preferences: ^2.5.5
  supabase_flutter: ^2.17.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
