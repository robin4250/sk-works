# Secret Scan CI probe

Temporary docs-only change used to verify that the always-on Secret Scan workflow runs even when Flutter/iOS workflows are path-filtered. This file is not intended to be merged.

Second docs-only commit to trigger a pull_request synchronize event after the workflow exists on main.
